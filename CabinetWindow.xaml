<Window x:Class="PCAdministration_.CabinetWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:PCAdministration_"
        mc:Ignorable="d"
        Title="CabinetWindow" Height="450" Width="800">
    <Grid Margin="25">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <!-- Поле: Имя / Наименование -->
        <StackPanel Grid.Row="0" Margin="0,0,0,15">
            <TextBlock Text="Имя / Наименование" Foreground="#4A4A4A" FontSize="13" FontWeight="SemiBold" Margin="0,0,0,5"/>
            <Border BorderBrush="#DCDCDC" BorderThickness="1" CornerRadius="5" Background="White">
                <TextBox x:Name="TxtName" Margin="5" Padding="3" BorderThickness="0" MaxLength="50" FontSize="14"/>
            </Border>
        </StackPanel>

        <!-- Поле: Этаж -->
        <StackPanel Grid.Row="1" Margin="0,0,0,20">
            <TextBlock Text="Этаж" Foreground="#4A4A4A" FontSize="13" FontWeight="SemiBold" Margin="0,0,0,5"/>
            <Border BorderBrush="#DCDCDC" BorderThickness="1" CornerRadius="5" Background="White" Width="100" HorizontalAlignment="Left">
                <TextBox x:Name="TxtFloor" Margin="5" Padding="3" BorderThickness="0" MaxLength="5" FontSize="14" HorizontalContentAlignment="Center"/>
            </Border>
        </StackPanel>

        <!-- Кнопки в стиле основного интерфейса -->
        <Grid Grid.Row="2" VerticalAlignment="Bottom">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="*"/>
            </Grid.ColumnDefinitions>

            <!-- Кнопка Отмена (Серая/светлая) -->
            <Button x:Name="BtnCancel" Content="Отмена" Grid.Column="0" Margin="0,0,10,0" Height="38" Click="BtnCancel_Click">
                <Button.Resources>
                    <Style TargetType="Border">
                        <Setter Property="CornerRadius" Value="8"/>
                    </Style>
                </Button.Resources>
                <Button.Style>
                    <Style TargetType="Button">
                        <Setter Property="Background" Value="#E0E0E4"/>
                        <Setter Property="Foreground" Value="#333333"/>
                        <Setter Property="BorderThickness" Value="0"/>
                        <Setter Property="FontWeight" Value="SemiBold"/>
                    </Style>
                </Button.Style>
            </Button>

            <!-- Кнопка Сохранить (Фиолетовая, как "Админ меню") -->
            <Button x:Name="BtnSave" Content="Сохранить" Grid.Column="1" Margin="10,0,0,0" Height="38" Click="BtnSave_Click">
                <Button.Resources>
                    <Style TargetType="Border">
                        <Setter Property="CornerRadius" Value="8"/>
                    </Style>
                </Button.Resources>
                <Button.Style>
                    <Style TargetType="Button">
                        <Setter Property="Background" Value="#9D66FF"/>
                        <Setter Property="Foreground" Value="White"/>
                        <Setter Property="BorderThickness" Value="0"/>
                        <Setter Property="FontWeight" Value="Bold"/>
                    </Style>
                </Button.Style>
            </Button>
        </Grid>
    </Grid>
</Window>
